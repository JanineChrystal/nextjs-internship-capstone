/**
 * How many notifications load at a time.
 *
 * Deliberately the same as COMMENTS_PAGE_SIZE: both are "a screenful plus a bit"
 * and there is no reason for two different answers to the same question.
 */
export const NOTIFICATIONS_PAGE_SIZE = 20;
