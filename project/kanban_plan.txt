<USER_REQUEST>
let's finalize this initial plan of mine for the @[app/(dashboard)/projects/_components/views/kanban-board/kanban-board.tsx] . let's scan first the updated related files to this changes then we create implementation plan. include in the implementation plan that we have to adhere with @[.agents/AGENTS.md] and @[GEMINI.md] so that we will refactor less in the future.

Here is your updated, comprehensive Kanban modification master context, now including the advanced bi-directional synchronization logic we just mapped out.

You can use this as your definitive checklist for the Kanban development sprint.

---

### Phase 1: Task Card UI & Metadata (`task-card.tsx`)

The task card requires a structural overhaul to support bulk selection, better metadata visibility, and a refined interaction pattern.

* **Bulk Selection Checkbox:** Add a checkbox at the top-left of the card. Its checked state and `onChange` handler will connect to the new selection state in `useTaskStore`.
* **Title as Modal Trigger:** Remove the `Edit2` icon. Wrap the task `title` in a button/hyperlink that triggers `openTaskModal(task.id)` from `useTaskStore`.
* **Metadata Layout Enhancement:** Reorganize the footer section to display:
* Assignee (User circle profile).
* Comments (Icon + count).
* Attachments (Icon + sum of document and link counts).
* Checklists (Icon + completed/total).
* Calendar (Icon + due date).
* Keep the existing `TaskBadge` for priority and category.



### Phase 2: Bulk Action Bar & State Management (`use-task-store.ts`)

Leverage the existing grid task store to handle multi-card actions.

* **Extend `useTaskStore`:** Add a `selectedTaskIds: Set<string>` property to the state, along with `toggleTaskSelection` and `clearTaskSelection` actions.
* **Action Bar Component:** Create a floating `BulkActionBar` component.
* **Visibility Logic:** Render this bar at the bottom of the screen only when `selectedTaskIds.size > 1`.
* **Hook Up Actions:** Wire the action bar buttons directly to your existing `bulkCompleteTasks(ids)` and `bulkDeleteTasks(ids)` functions. Clear the selection state after the action fires.

### Phase 3: Column Redesign & "Completed" Section (`column.tsx`)

The column needs to separate active tasks from completed tasks within the same vertical lane.

* **Task Partitioning:** Inside the `Column` component, filter the incoming `tasks` prop into `activeTasks` and `completedTasks`.
* **Active Tasks:** Wrap only the `activeTasks` in the existing `SortableContext`.
* **Completed Tasks Accordion:** Below the active tasks, implement a collapsible section (e.g., Shadcn `Collapsible`). When expanded, map through the `completedTasks`.
* **Column Dropdown Menu:** Replace the static `MoreHorizontal` button with a `DropdownMenu` component containing "Rename Board" and "Delete Board" actions.

### Phase 4: Board Management & Drag-and-Drop (`board-store.ts` & `kanban-board.tsx`)

Activate the placeholder `useBoardStore` to handle list and DnD logic.

* **Implement `useBoardStore`:** Fulfill the store requirements to manage `lists` (columns), `draggedTask`, and the drag-and-drop position handlers.
* **Draggable Columns:**
* Wrap the mapping of the columns array in `kanban-board.tsx` in a horizontal `SortableContext`.
* Update the `Column` component to utilize `useSortable` (instead of just `useDroppable`) so the entire board lane can be dragged.


* **Add New Board:** Append an "Add Board" button at the end of the mapped columns list that pushes a new column object to your store.

### Phase 5: Toolbar Filter Integration

Ensure the Kanban board respects the project toolbar filters.

* **Filter Application:** In `kanban-board.tsx`, before mapping tasks to their respective columns, apply a filtering function based on your global filter state (e.g., search term, assignee, priority).
* **Prop Drilling Tasks:** Pass the *filtered* array of tasks down to the `Column` components to ensure hidden tasks cannot be interacted with or dragged.

### Phase 6: Bi-Directional State Synchronization (Board Columns â†” Task Status)

Ensure that task statuses and board columns remain perfectly in sync regardless of where the update occurs.

* **Dynamic Status Options:** Update all task status dropdowns (in Modals, Grid view, etc.) to derive their available options directly from the current list of Board Columns. If a column is renamed or added, the status options update automatically.
* **Direction 1 (Drag & Drop Relocation):** Update the DnD drop handler so that dragging a task into a new column updates both `task.columnId` AND overwrites `task.status` to match the target column's name.
* **Direction 2 (Status Update Relocation):** Update the `updateTaskStatus` handler so that if a user changes a status via a dropdown or bulk action (e.g., marking as "Completed"), the system automatically updates the `task.columnId` to move the card to the corresponding board column.
* **Safe Column Deletion Fallback:** When a user deletes a column, implement logic to migrate any active tasks inside it to a default column (like *Backlog* or *To Do*) to prevent orphaned data.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-12T13:11:10+08:00.

The user's current state is as follows:
Active Document: c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project\app\(dashboard)\_constants\warning-modal.ts (LANGUAGE_TYPESCRIPT)
Cursor is on line: 1
Other open documents:
- c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project\app\(dashboard)\calendar\_constants\mock-data.ts (LANGUAGE_TYPESCRIPT)
- c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project\app\(dashboard)\calendar\_hooks\use-calendar-page.ts (LANGUAGE_TYPESCRIPT)
- c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project\app\(dashboard)\_components\ui\modals\project-modal\index.ts (LANGUAGE_TYPESCRIPT)
- c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project\app\(dashboard)\_components\ui\cards\deadline-card.tsx (LANGUAGE_TSX)
- c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project\app\layout.tsx (LANGUAGE_TSX)
Running terminal commands:
- pnpm ngrok.exe http 3000 (in c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project, running for 3h50m16s)

The user has mentioned some items in the form @[ITEM]. Here is extra information about the items that were mentioned by the user, in the order that they appear:

@[app/(dashboard)/projects/_components/views/kanban-board/kanban-board.tsx] is a [File]:
c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project\app\(dashboard)\projects\_components\views\kanban-board\kanban-board.tsx
@[.agents/AGENTS.md] is a [File]:
c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project\.agents\AGENTS.md
@[GEMINI.md] is a [File]:
c:\Users\chrys\janine-kanban\nextjs-internship-capstone\project\GEMINI.md
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Gemini 3.6 Flash (High) to Gemini 3.1 Pro (Low). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>
